name: Packages Build
on:
  push:
    branches:
      - develop
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
jobs:
  build-everything:
    name: All Packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: setup-node@v2
        with:
          node-version: 14.x
      - run: npm ci
      - run: npm run build --workspaces
  